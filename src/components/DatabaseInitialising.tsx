import { Text, View } from 'react-native';

const DatabaseInitialising = () => {
  return (
    <View style={{ alignItems: 'center', justifyContent: 'center', height: '100%' }}>
      <Text>Database initialising...</Text>
    </View>
  );
};

export default DatabaseInitialising;
